description: NotFoundError
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          type: integer
          example: 404
        message:
          type: string
          example: User not found!
          description: NotFoundError
        errorCode:
          type: string
          example: USER_NOT_FOUND
