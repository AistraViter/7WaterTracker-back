description: DuplicateResourceError
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
      properties:
        status:
          type: integer
          example: 409
          description: DuplicateResourceError
        message:
          type: string
          example: 'Email in use'
          description: DuplicateResourceError
